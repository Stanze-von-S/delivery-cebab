  <div class="radiusSearchGroup">
    <p>Радиус поиска:
      <input type="range" min="1" max="100" step="1" name="radiusSearch" value="50">
    </p>
  </div>

  <div id="map"></div>

<p>Категории</p>

<ul class="list">
  {{#each categories}}
<li class="listItem">
<input data-category_index="{{this.id}}" name="my_btn" class="appendBtn btn btn-outline-success" type="button"
      value="{{this.title}}">
      </li>
      {{/each}}
</ul>

